const Client = require('./lib/Client.js');

let rid = 0;
const service = {};
const queue = [];
const requestQueue = new Map();

function start(ready) {
    const client = new Cliet();
    function send() {
        
    }
}
